= A juicy start
:toc:
:toc-placement: preamble
:icons: font

== To Prompt or not to Prompt

One of the first layers of safety is known as the system prompt. 

System prompts are specific instructions that LLMs process that includes information about what they should and should not talk about. 
You can also use it to dictate how the LLM should portray itself, with "talking like a pirate" as a common example, and how information should be delivered to the user. 

_You decide to take a look at the current system prompt in order determine whether or not its instructions are sufficient._ 

== First quest: Find the current system prompt

PeachPal is running in OpenShift, its system prompt exists as a ConfigMap so it is easy to view and update without having to rebuild the front end. 
This allows us to make fast changes without disrupting the rest of the application. Clearly, the application developers creating it thought ahead.

_You put it on your to do list to thank them for not having to re-program, build and deploy the container every time you want to do a small update._

Let's login to OpenShift (again, open it up on a new tab)


{openshift_console_url}[OpenShift Console,window=_blank]

image::OCP-login.png[OCP Login]

** username:
+
[source,text,role=execute,subs="attributes"]
----
{user}
----
** password :
+
[source,text,role=execute,subs="attributes"]
----
{password}
----

_You are greeted with the following launcher and find it optional to do the tour, you may skip it._ 
image:launcher.png[Launcher]

Behind the launcher awaits the namespace in which the application is deployed.

_You follow the red bubbles in the below image to get to the configmaps._
[TIP]
====
If you see "Restricted access" error, check the image below and make sure that you picked {user}-peachpal project on the top.
====
image:step-to-systemprompt.png[Getting there]


_Your gaze hovers over the several ConfigMaps(CM) that exist in the namespace. Which one contains the values for PeachPals system prompt?_

_You press the name of that ConfigMap_
image:configmaps.png[ConfigMap]

_Success! You found the system prompt ConfigMap and you see the detail view._ 
_Your eyes scroll down to the value "prompt", and in plain text you see the LLMs instructions._ 
image:systemprompt.png[SystemPrompt]

**You gained 50 XP** 

== Second quest: Update the system prompt
As you have now understood, the instructions "You are a helpful assistant" is far from enough and allow for many topics unrelated to peaches. 
Here, we have the possibility to make a difference. 

Now, we need to add in plain text, more concrete instructions of what it SHOULD talk about and what NOT to talk about.
It is also needed to instruct the LLM on how to behave and respond if it comes in contact with a forbidden topic. 
We can add those instructions into the ConfigMap in the prompt values, building on the "helpful assistant", since we still want it to be helpful for our users.




_You click the button "Actions, followed by "Edit ConfigMap"._ 
image:updated-prompt.png[Update Prompt]

[TIP]
====
Add these instructions to the system prompt.

[source]
----
You are a helpful assistant specialized in peaches, working for a company called Peach Happens Produce.
You will only talk about peaches, no other topics are allowed.
If someone asks about another topic, politely decline and redirect the topic back to peaches.
----
====
image:updated-prompt2.png[Save the new prompt]

Scroll down to see the new system prompt in the view. 


Because ConfigMaps are not automatically loaded, we need to restart the frontend pod of PeachPal. 
We have updated the prompt values, but the container process keeps the old configuration that it loaded when it first started. 
When we restart the pod, it will reload the ConfigMap and therefore will get the new values. 

_You press the "Pods" in the left hand menu, find the pod running PeachPal, click the 3 dots and press "Delete Pod"._
image:restart-UI-pod.png[Restart UI]

_You press delete when prompted again._ 
image:delete-pod.png[Delete pod]
After deleting the pod, you will see another spinning up instantly. That is because the developers have defined the: restartPolicy: Always". 
This means that you don't need to redeploy anything, you can just delete or kill the pod. Sweet, right? 

When its ready, you will see "Ready" being 1/1 again. 

That means, it's now time to test the application again by asking it question about non-peach related questions. Take 2 minutes. 
If you saved the PeachPal user tab, click it. If you didn't, here is the link again. Come back to the instructions when you are satisfied. 


https://peachpal-{user}-peachpal.{openshift_cluster_ingress_domain}

[TIP]
====
**Ask it again:** 

"Recommend me other fruits than peaches"

"Give me a C++ script for a Hello World"

"How do I start an Airbus 350?"
====

Did PeachPal provide the answers to the above questions to you? No? Fantastic!

**You gained 50 XP**

== Level up!

**You gained the Skill: LLM-tamer** 

With the power of plain text, you were able to steer and instruct the LLM on its behaviour and replies.

_You lean back in your chair, satisfied that PeachPal now no longer answers questions about unrelated topics, talks about other fruits or instructs you on how to start an airplane._
_No longer will you need to listen to Robs Malaga vacation plans he got from PeachPal._  

== Summary: The Reality of Prompt Engineering

**TL;DR** Prompt Engineering is hard and not a 100% guarantee to block all non-allowed topics.


With the addition of 3-4 simple system prompt instructions, we are able to move the topic back to peaches. We have manually added them into the ConfigMap, and manually asked it 3-4 questions via the frontend.


But, the reality is that finding the optimal prompt is an intense and iterative task that needs to be statistically evaluated. This is known as Prompt Engineering. The same system prompt may not work the same for different LLMs and a validated, carefully curated dataset of questions or tasks is needed. 
Every time the system prompt is changed the output needs to be validated and every time the model changes the system prompt needs to be evaluated. 

So, imagine that you wanted to evaluate 5 different models and 100 different system prompts with a much larger validated dataset in order to find the most optimal combination? 
This would not be something you would do manually. You would need to create pipelines and automation to do the test and collate all the data.

The amount of system prompt instructions are also dependent on the LLM being used, and the prompts can be very long.

For example, did you know that Claudes (a well known AI-coding assistant) prompt is about 17 000 words long? That's a bit more than the one we added to PeachPal. 

As you have now understood, the first layer of defence is the LLM system prompt since it derives its instructions from there. 
However, our lab does not end here. 

For you will soon discover that while the system prompt is the first layer, it is not enough.

'''
xref:workshop/guardrails/03-module-01.adoc[Next: The Forbidden Fruits ->]
